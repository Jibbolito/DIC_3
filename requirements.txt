# Core AWS dependencies
boto3

# NLP libraries for preprocessing
nltk

# Profanity detection (as recommended in tips & tricks)
profanityfilter

# AWS CLI tools
awscli
awscli-local

# Testing
pytest
moto

# Utilities
requests
packaging
regex

# LocalStack - install separately on Windows using install_requirements.py
# due to Windows long path issues. On Linux/Mac, install with:
# pip install localstack localstack-client